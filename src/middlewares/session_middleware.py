# from src.database import Session
#
#
# @web.middleware
# async def session_middleware(request, handler):
#     request.session = your_session_creation_method()
#     response = await handler(request)
#     return response
